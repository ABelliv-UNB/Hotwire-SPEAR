<UserControl x:Class="Hotwire_Transient_GUI.MVVM.View.PointsDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hotwire_Transient_GUI.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Style="{StaticResource CardTheme}">

        <Grid>
            <ListView 
                Grid.Row="1" 
                ItemsSource="{Binding Data}"
                Background="{DynamicResource CardColor}"
                ItemContainerStyle="{StaticResource PointsDisplayTheme}"
                Margin="10"
                >


            </ListView>

        </Grid>
        
        
    </Border>
    
</UserControl>
